---
const { class: className = "", ...rest } = Astro.props;
---

<svg
  id="bigLogo"
  class={className}
  {...rest}
  viewBox="0 0 900 300"
  xmlns="http://www.w3.org/2000/svg"
  role="img"
  aria-labelledby="mtmTitle mtmSubtitle"
>
  <title id="mtmTitle">Making the Metaverse</title>
  <desc id="mtmSubtitle">
    A podcast by Michael Carter about AI, virtual worlds, and the future of work.
  </desc>

  <!-- Main title, stacked for more impact -->
  <text
    x="0"
    y="120"
    font-family="system-ui, -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Helvetica Neue', Arial, sans-serif"
    font-size="96"
    font-weight="700"
    letter-spacing="1"
    fill="currentColor"
  >
    <tspan x="0" dy="0">Making the</tspan>
    <tspan x="0" dy="90">Metaverse</tspan>
  </text>

  <!-- Subtitle -->
  <text
    x="0"
    y="260"
    font-family="system-ui, -apple-system, BlinkMacSystemFont, 'SF Pro Text', 'Helvetica Neue', Arial, sans-serif"
    font-size="28"
    font-weight="400"
    fill="currentColor"
    opacity="0.85"
  >
    A podcast by Michael Carter
  </text>
</svg>

<script type="module">
  const path = window.location.pathname;
  // Only show the big logo on primary landing pages
  if (
    path !== "/" &&
    path !== "/index-2" &&
    path !== "/podcast/home"
  ) {
    document.getElementById("bigLogo")?.style.setProperty("display", "none");
  }
</script>
